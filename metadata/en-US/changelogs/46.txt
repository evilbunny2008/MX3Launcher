* Settings backup/restore now only needs pairing once, then a single
  tap for each backup/restore. Manage or revoke pairings from the
  pairing site.
* Soundbar-wake pairing now carries its own IR code and power-check
  setting, so it can target different soundbars/setups.
* Phone-side pairing approvals can now remember your choice for
  one-tap future approvals.
* Fixed soundbar-wake pairing sometimes appearing to succeed then
  silently reverting to unpaired.
