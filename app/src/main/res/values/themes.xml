<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Platform-only parent theme: no AppCompat/Material Components
      dependency (MainActivity is plain ComponentActivity; all UI is
      Compose via LauncherTheme.kt), so this only needs window-level
      basics, not widget styling.
    -->
    <style name="Theme.MX3Launcher" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
