<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- For the soundbar-wake HTTP call in AppGridScreen.kt -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ForegroundServicesPolicy" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
      Backs the full-screen-intent fallback in ScreenWakeGuardService.kt
      (see bringLauncherToFront() there for the Android 14+ grant caveat).
    -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"
        tools:ignore="FullScreenIntentPolicy" />

    <!--
      A launcher needs to see every installed app. &lt;queries&gt; is the
      Play-Store-compliant way to do that on API 30+, vs. the more heavily
      gated QUERY_ALL_PACKAGES; without it, queryIntentActivities() returns
      nothing for other packages.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
        </intent>
    </queries>

    <application
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:banner="@drawable/banner"
        android:theme="@style/Theme.MX3Launcher"
        android:supportsRtl="true">

        <!--
          This is what makes the app selectable as a Home/launcher
          replacement. CATEGORY_LEANBACK_LAUNCHER additionally marks it as
          TV-optimised, so Android TV/Google TV recognises and prioritises
          it as such over a generic phone-style launcher.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Foreground service type required as of Android 14; no specific FGS
          type fits "watch for screen-on and bring the launcher back", so
          this uses specialUse with a justification property.
        -->
        <service
            android:name=".ScreenWakeGuardService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Detects device wake from standby and restores the Home launcher, since Android may kill the launcher's own process while the screen is off" />
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
